<Window x:Class="QuanLyPassword_V1.AddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thêm tài khoản" Height="320" Width="400" WindowStartupLocation="CenterOwner"
        Icon="/img/dakirby309_simply_styled_microsoft_security_essentials_256_qDp_icon.ico">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Loại tài khoản:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
        <ComboBox x:Name="cbAccountType" Grid.Row="0" Grid.Column="1" Margin="0,5">
            <ComboBoxItem Content="Google"/>
            <ComboBoxItem Content="Facebook"/>
            <ComboBoxItem Content="Twitter"/>
            <ComboBoxItem Content="LinkedIn"/>
            <ComboBoxItem Content="Instagram"/>
            <ComboBoxItem Content="Social"/>
            <ComboBoxItem Content="Email"/>
            <ComboBoxItem Content="Banking"/>
            <ComboBoxItem Content="Work"/>
            <ComboBoxItem Content="Github"/>
            <ComboBoxItem Content="Other"/>
        </ComboBox>

        <TextBlock Text="Tên đăng nhập:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox x:Name="txtUsername" Grid.Row="1" Grid.Column="1" Margin="0,5"/>

        <TextBlock Text="Mật khẩu:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5"/>
        <Grid Grid.Row="2" Grid.Column="1" Margin="0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="30"/>
            </Grid.ColumnDefinitions>

            <PasswordBox x:Name="pwdPassword" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox x:Name="txtPasswordVisible" Grid.Column="0" VerticalAlignment="Center" Visibility="Collapsed"/>

            <Button Grid.Column="1" Width="25" Height="25" VerticalAlignment="Center" Click="BtnTogglePassword_Click" ToolTip="Hiện/Ẩn mật khẩu" Background="Transparent" BorderThickness="0" Padding="0">
                <TextBlock x:Name="txtEyeIcon" Text="👁" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Button>
        </Grid>

        <TextBlock Text="Ghi chú:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Top" Margin="0,5"/>
        <TextBox x:Name="txtNote" Grid.Row="3" Grid.Column="1" Margin="0,5" Height="100" VerticalContentAlignment="Top"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button Content="Lưu" Width="140" Height="30" Click="BtnSave_Click" Margin="0,0,10,0"/>
            <Button Content="Hủy" Width="140" Height="30" Click="BtnCancel_Click" Margin="10,0,0,0"/>
        </StackPanel>

    </Grid>
</Window>
