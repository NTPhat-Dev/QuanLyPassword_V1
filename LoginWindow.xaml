<Window x:Class="QuanLyPassword_V1.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng nhập" Height="280" Width="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Icon="/img/dakirby309_simply_styled_microsoft_security_essentials_256_qDp_icon.ico">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="Tên đăng nhập:" FontWeight="Bold" VerticalAlignment="Center"
                   Grid.Row="0" Grid.Column="0" Margin="0,0,10,0" />
        <TextBox x:Name="txtUsername" Grid.Row="0" Grid.Column="1" Height="25" />

        <TextBlock Text="Mật khẩu:" FontWeight="Bold" VerticalAlignment="Center"
                   Grid.Row="1" Grid.Column="0" Margin="0,6,10,0" />
        <PasswordBox x:Name="pwdPassword" Grid.Row="1" Grid.Column="1" Height="25" Margin="0,6,0,0" />
        <TextBox x:Name="txtPasswordVisible" Grid.Row="1" Grid.Column="1" Height="25" Margin="0,6,0,0" Visibility="Collapsed" />

        <TextBlock x:Name="txtMessage" Grid.Row="2" Grid.ColumnSpan="2" Foreground="Red" Margin="0,6,0,6" TextWrapping="Wrap" Visibility="Collapsed" />

        <CheckBox x:Name="chkShowPassword" Grid.Row="3" Grid.ColumnSpan="2" Content="Hiện mật khẩu" Margin="0,6,0,10" HorizontalAlignment="Left"
                  Checked="ChkShowPassword_Checked" Unchecked="ChkShowPassword_Unchecked" />

        <CheckBox x:Name="chkRememberMe" Grid.Row="4" Grid.ColumnSpan="2" Content="Ghi nhớ đăng nhập" Margin="0,0,0,15" HorizontalAlignment="Left" />

        <Button x:Name="btnLogin" Content="Đăng nhập" Grid.Row="5" Grid.ColumnSpan="2" Height="30" Background="#007ACC"
                Foreground="White" FontWeight="Bold" HorizontalAlignment="Stretch" Click="BtnLogin_Click" />

        <TextBlock Grid.Row="6" Grid.ColumnSpan="2" Margin="0,10,0,0" HorizontalAlignment="Left" FontSize="12">
            <Run Text="Chưa có tài khoản? " />
            <Hyperlink Click="RegisterLink_Click" Foreground="Blue" Cursor="Hand" TextDecorations="Underline">
                Đăng ký
            </Hyperlink>
        </TextBlock>

        <TextBlock Grid.Row="7" Grid.ColumnSpan="2" Margin="0,6,0,0" HorizontalAlignment="Left" FontSize="12">
            <Hyperlink Click="ForgotPasswordLink_Click" Foreground="Blue" Cursor="Hand" TextDecorations="Underline">
                Quên mật khẩu?
            </Hyperlink>
        </TextBlock>
    </Grid>
</Window>
